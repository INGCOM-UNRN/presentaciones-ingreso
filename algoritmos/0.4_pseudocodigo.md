---
marp: true
theme: UNRN
paginate: false
header: 'UNRN Andina'
footer: 'Curso de Ingreso - Ingeniería en Computación - UNRN Andina'
size: 4:3
---

<!-- _class: lead -->
<!-- _footer: 'Primeros Pasos - Parte 4/5' -->

# <!-- fit --> Pseudocódigo
## Español estructurado
Curso de Ingreso - Ingeniería en Computación

<!--
NOTAS DEL ORADOR:
- Duración estimada: 3 minutos
- Objetivo: Aprender a escribir algoritmos en texto sin preocuparse por la sintaxis estricta.
- Gancho: "¿Y si pudiéramos programar en español, sin puntos y comas obligatorios? Eso es el pseudocódigo."
-->

---

<!-- _header: '¿Qué es?' -->

# Pseudocódigo

**Definición:**
* Escribir el algoritmo en "español estructurado"
* Parecido a código real pero legible
* Punto medio entre humano y máquina

**Ventaja:**
* No requiere sintaxis exacta
* Se enfoca en la lógica
* Fácil de traducir a cualquier lenguaje

<!--
NOTAS DEL ORADOR:
- "Pseudo" = Falso. Pero no por malo, sino por no ser ejecutable.
- Es agnóstico del lenguaje. Sirve para Python, Java o C.
-->

---

<!-- _header: 'Palabras clave' -->

# Vocabulario del pseudocódigo

**Básicas:**
* `INICIO` / `FIN` - Marcan comienzo y final
* `LEER` - Pedir datos al usuario
* `MOSTRAR` - Mostrar datos al usuario
* `⟸` - Asignación (guardar un valor)

**Decisiones:**
* `SI ... ENTONCES` - Condicional
* `SINO` - Camino alternativo
* `FIN_SI` - Cierre de condicional

<!--
NOTAS DEL ORADOR:
- La flecha `⟸` es importante para distinguir asignación de igualdad.
- Palabras en mayúsculas para diferenciar las instrucciones de las variables.
-->

---

<!-- _header: 'Palabras clave' -->

# Vocabulario: Lazos

**Repeticiones:**
* `MIENTRAS ... HACER` - Repetir mientras condición
* `FIN_MIENTRAS` - Cierre del lazo
* `PARA ... DESDE ... HASTA` - Lazo con contador
* `FIN_PARA` - Cierre del lazo for

**Comentarios:**
* `#` - Comentario explicativo

<!--
NOTAS DEL ORADOR:
- Los bloques siempre se cierran explícitamente (`FIN_...`) para evitar ambigüedad en papel.
-->

---

<!-- _header: 'Estructura' -->

# Plantilla básica

```
INICIO NombreDelAlgoritmo
    # Acá van los pasos
FIN
```

**Siempre:**
* Comienza con INICIO
* Termina con FIN
* Los pasos van indentados (con espacios)

<!--
NOTAS DEL ORADOR:
- La indentación visual es clave para la legibilidad.
-->

---

<!-- _header: 'Ejemplo 1' -->

# Sumar dos números

```
INICIO SumarDosNumeros
    # Pedir los números
    MOSTRAR "Ingrese el primer número:"
    LEER numero1
    
    MOSTRAR "Ingrese el segundo número:"
    LEER numero2
    
    # Hacer la suma
    suma ⟸ numero1 + numero2
    
    # Mostrar el resultado
    MOSTRAR "La suma es:", suma
FIN
```

---

<!-- _header: 'Ejemplo 2' -->

# Mayor de edad

```
INICIO MayorDeEdad
    # Pedir edad
    MOSTRAR "Ingrese su edad:"
    LEER edad
    
    # Verificar
    SI edad >= 18 ENTONCES
        MOSTRAR "Sos mayor de edad"
    SINO
        MOSTRAR "Sos menor de edad"
    FIN_SI
FIN
```

<!--
Notá la indentación dentro del bloque SI-SINO. Esto es importante para ver qué código se ejecuta en cada caso.
-->

---

<!-- _header: 'Detalles' -->

# Estructura SI-ENTONCES-SINO

**Observá:**
* El `SI` tiene un `ENTONCES` al final
* Lo que está dentro está indentado
* Hay un `SINO` para el caso contrario
* Cierra con `FIN_SI`

**Indentación:**
* Usa espacios para mostrar jerarquía
* Python usa esto en el código real

<!--
NOTAS DEL ORADOR:
- Explicar la jerarquía visual.
-->

---

<!-- _header: 'Ejemplo 3' -->

# Contar del 1 al 5

```
INICIO ContarHastaCinco
    # Empezar desde 1
    contador ⟸ 1
    
    # Repetir mientras sea <= 5
    MIENTRAS contador <= 5 HACER
        MOSTRAR contador
        contador ⟸ contador + 1
    FIN_MIENTRAS
    
    MOSTRAR "¡Terminé de contar!"
FIN
```

<!--
**Este es el ejemplo más complejo hasta ahora:** introduce **lazos/bucles** (MIENTRAS).

Ahora el algoritmo puede repetir acciones múltiples veces, no solo ejecutar una secuencia lineal o tomar una decisión.

**Análisis del código:**

1. **Inicialización:**
   ```
   contador ⟸ 1
   ```
   - Antes de entrar al lazo, inicializamos la variable de control
   - Sin esto, `contador` no tendría valor definido
   - El contador empieza en 1 porque queremos contar del 1 al 5

2. **Estructura del lazo:**
   ```
   MIENTRAS contador <= 5 HACER
       MOSTRAR contador
       contador ⟸ contador + 1
   FIN_MIENTRAS
   ```

**Funcionamiento paso a paso:**

- **Antes del lazo:** contador = 1
- **Iteración 1:** ¿1 <= 5? Sí → Muestra 1, contador pasa a 2
- **Iteración 2:** ¿2 <= 5? Sí → Muestra 2, contador pasa a 3
- **Iteración 3:** ¿3 <= 5? Sí → Muestra 3, contador pasa a 4
- **Iteración 4:** ¿4 <= 5? Sí → Muestra 4, contador pasa a 5
- **Iteración 5:** ¿5 <= 5? Sí → Muestra 5, contador pasa a 6
- **Iteración 6:** ¿6 <= 5? No → Sale del lazo

3. **Después del lazo:**
   ```
   MOSTRAR "¡Terminé de contar!"
   ```
   - Esta línea se ejecuta UNA VEZ cuando el lazo termina
   - Está fuera del FIN_MIENTRAS

**Elementos críticos del lazo:**

1. **Inicialización:** contador ⟸ 1 (antes del MIENTRAS)
2. **Condición:** contador <= 5 (evalúa ANTES de cada iteración)
3. **Cuerpo:** MOSTRAR contador (lo que se repite)
4. **Actualización:** contador ⟸ contador + 1 (CRUCIAL)
5. **Terminación:** FIN_MIENTRAS (marca dónde termina el bloque)

**Error común (lazo infinito):**
```
MIENTRAS contador <= 5 HACER
    MOSTRAR contador
    # ¡FALTA! contador ⟸ contador + 1
FIN_MIENTRAS
```
Sin incrementar contador, siempre será 1, la condición siempre será verdadera, y el lazo nunca terminará.

**Correspondencia con diagrama de flujo:**
- contador ⟸ 1 → Rectángulo de proceso
- MIENTRAS → Rombo de decisión
- Cuerpo del lazo → Rectángulos dentro del lazo
- Flecha hacia atrás → De FIN_MIENTRAS a MIENTRAS

**Traducción a Python:**
```python
contador = 1
while contador <= 5:
    print(contador)
    contador = contador + 1
print("¡Terminé de contar!")
```

**Pregunta reflexiva:** "¿Qué pasaría si iniciamos contador en 0 en vez de 1?" (Mostraría 0, 1, 2, 3, 4, 5 → 6 números en vez de 5).

**Actividad:** Pedí a un estudiante que ejecute este pseudocódigo mentalmente, diciendo en voz alta el valor de contador en cada iteración y cuándo sale del lazo.
-->

---

<!-- _header: 'Detalles' -->

# Estructura MIENTRAS

**Observá:**
* `MIENTRAS ... HACER` es un lazo
* Todo lo indentado se repite
* `contador ⟸ contador + 1` es crucial
* Sin esto, lazo infinito
* Cierra con `FIN_MIENTRAS`

<!--
NOTAS DEL ORADOR:
- El incremento manual es un detalle que a menudo se olvida en papel.
-->

---

<!-- _header: 'Ejemplo 4' -->

# Promedio de 3 números

```
INICIO PromedioDeTres
    # Entrada
    MOSTRAR "Ingrese tres números:"
    LEER num1, num2, num3
    
    # Proceso
    suma ⟸ num1 + num2 + num3
    promedio ⟸ suma / 3
    
    # Salida
    MOSTRAR "El promedio es:", promedio
FIN
```

<!--
**Cuarto y último ejemplo:** Promedio de 3 números. Regresamos a un algoritmo secuencial (sin decisiones ni lazos) pero más completo.

**Este ejemplo refuerza la estructura Entrada-Proceso-Salida.**

**Análisis detallado:**

1. **Sección de Entrada:**
   ```
   # Entrada
   MOSTRAR "Ingrese tres números:"
   LEER num1, num2, num3
   ```
   
   **Novedad:** Leer **múltiples variables en una línea**
   - `LEER num1, num2, num3` es válido en pseudocódigo
   - Significa "leer tres valores y asignarlos a num1, num2, num3"
   - En Python: necesitarías 3 líneas de `input()` o parsear una línea

2. **Sección de Proceso (dos pasos):**
   ```
   # Proceso
   suma ⟸ num1 + num2 + num3
   promedio ⟸ suma / 3
   ```
   
   **Dividido en sub-pasos:**
   - Primero calculamos la suma
   - Luego dividimos la suma entre 3
   - Podríamos hacerlo en una línea: `promedio ⟸ (num1 + num2 + num3) / 3`
   - Pero dividirlo es más claro (especialmente para principiantes)

3. **Sección de Salida:**
   ```
   # Salida
   MOSTRAR "El promedio es:", promedio
   ```
   - Mostramos texto descriptivo + el valor calculado

**Observaciones pedagógicas:**

**Comentarios organizadores:**
- `# Entrada`, `# Proceso`, `# Salida`
- Ayudan a identificar las tres partes fundamentales
- En código real, estos comentarios son muy útiles para organizar

**Variables intermedias:**
- `suma` es una variable intermedia
- No es estrictamente necesaria, pero hace el código más legible
- **Legibilidad > brevedad** (principio fundamental de programación)

**Conexión con las 5 Preguntas Mágicas:**

1. ¿Qué datos necesito? → num1, num2, num3
2. ¿Qué resultado quiero? → promedio
3. ¿Qué pasos debo seguir? → Leer, sumar, dividir, mostrar
4. ¿Hay situaciones especiales? → No (asumimos siempre 3 números)
5. ¿Necesito repetir algo? → No

**Este algoritmo responde perfectamente las 5 preguntas.**

**Comparación de estilos:**

*Estilo compacto (menos claro):*
```
INICIO Promedio
    LEER a, b, c
    MOSTRAR (a + b + c) / 3
FIN
```

*Estilo detallado (más claro):*
```
INICIO PromedioDeTres
    # Entrada
    MOSTRAR "Ingrese tres números:"
    LEER num1, num2, num3
    
    # Proceso
    suma ⟸ num1 + num2 + num3
    promedio ⟸ suma / 3
    
    # Salida
    MOSTRAR "El promedio es:", promedio
FIN
```

**Ambos son correctos, pero el segundo es más profesional.**

**Traducción a Python:**
```python
print("Ingrese tres números:")
num1 = float(input())
num2 = float(input())
num3 = float(input())

suma = num1 + num2 + num3
promedio = suma / 3

print("El promedio es:", promedio)
```

**Actividad:** Pedí que ejecuten mentalmente con valores: num1=10, num2=20, num3=30. Resultado esperado: promedio=20.

**Siguiente slide:** Generalizamos las tres partes de TODO algoritmo.
-->

---

<!-- _header: 'Partes' -->

# Las tres partes

Todo algoritmo tiene:

**1. Entrada (📥):**
* LEER datos del usuario

**2. Proceso (⚙️):**
* Cálculos y operaciones

**3. Salida (📤):**
* MOSTRAR resultados

<!--
NOTAS DEL ORADOR:
- Refuerzo del modelo E-P-S.
-->

---

<!-- _header: 'Comparación' -->

# Pseudocódigo vs Python

**Pseudocódigo:**
```
INICIO Saludar
    MOSTRAR "Hola Mundo"
FIN
```

**Python:**
```python
def saludar():
    print("Hola Mundo")
```

**Similar pero no igual**

<!--
NOTAS DEL ORADOR:
- Python es casi pseudocódigo ejecutable. Por eso es tan bueno para aprender.
-->

---

<!-- _header: 'Comparación' -->

# Estructuras equivalentes

| Pseudocódigo | Python |
|:-------------|:-------|
| `LEER x` | `x = input()` |
| `MOSTRAR x` | `print(x)` |
| `x ⟸ 5` | `x = 5` |
| `SI ... ENTONCES` | `if ...:` |
| `MIENTRAS ... HACER` | `while ...:` |

---

<!-- _header: 'Tips' -->

# Consejos para pseudocódigo

**Sé claro:**
* Escribe como si explicaras a alguien
* No uses abreviaturas raras

**Usa indentación:**
* Muestra qué está dentro de qué

**Comenta:**
* Explica secciones complejas
* Usa `#` para comentarios

<!--
Este slide ofrece **consejos prácticos** para escribir buen pseudocódigo.

**1. Sé claro (claridad sobre brevedad):**

*Mal (abreviaturas):*
```
INICIO CalcProm
    L n1, n2, n3
    p ⟸ (n1+n2+n3)/3
    M p
FIN
```

*Bien (claro):*
```
INICIO CalcularPromedio
    LEER numero1, numero2, numero3
    promedio ⟸ (numero1 + numero2 + numero3) / 3
    MOSTRAR promedio
FIN
```

**¿Por qué?** El pseudocódigo es para humanos, no para computadoras. La claridad es prioritaria.

**2. Usa indentación (jerarquía visual):**

*Mal (sin indentar):*
```
SI edad >= 18 ENTONCES
MOSTRAR "Mayor"
SINO
MOSTRAR "Menor"
FIN_SI
```

*Bien (indentado):*
```
SI edad >= 18 ENTONCES
    MOSTRAR "Mayor"
SINO
    MOSTRAR "Menor"
FIN_SI
```

**¿Por qué?** La indentación muestra qué código pertenece a qué estructura. En Python, esto será obligatorio.

**3. Comenta (documenta el porqué):**

*Código sin comentarios:*
```
contador ⟸ contador + 1
```

*Código con comentario útil:*
```
# Avanzamos al siguiente elemento
contador ⟸ contador + 1
```

**Buenos vs malos comentarios:**

*Malo (obvio):*
```
suma ⟸ a + b  # sumamos a y b
```
El código ya dice QUÉ hace, el comentario es redundante.

*Bueno (explica porqué):*
```
suma ⟸ a + b  # Acumulamos el total para calcular promedio después
```
El comentario explica POR QUÉ lo hacemos, el código muestra CÓMO.

**Otros consejos:**

4. **Nombres de variables descriptivos:**
   - ✅ precio_total, edad_usuario, cantidad_items
   - ❌ x, y, z, temp, aux

5. **Líneas en blanco para separar secciones:**
   ```
   # Entrada
   LEER datos
   
   # Proceso
   calcular_algo
   
   # Salida
   MOSTRAR resultado
   ```

6. **Un paso por línea:**
   - Evita: `x ⟸ 5; y ⟸ 10; MOSTRAR x + y`
   - Mejor: cada acción en su propia línea

**Principio fundamental:** **El pseudocódigo se escribe una vez pero se lee muchas veces.** Invertir en claridad ahorra tiempo después.

**Analogía:** Es como escribir un ensayo: podés escribirlo todo en un párrafo sin puntuación, pero nadie lo leerá. Usar párrafos, sangrías y puntuación lo hace legible.

**Práctica recomendada:** Después de escribir pseudocódigo, léelo en voz alta. Si suena confuso, reescríbelo.

**Siguiente:** Ahora viene la práctica. Los estudiantes escribirán su propio pseudocódigo.
-->

---

<!-- _class: inverse -->

# <!-- fit --> Práctica
## Escribí tus propios algoritmos

<!--
Este slide marca la **transición de teoría a práctica**. Ahora los estudiantes aplicarán lo aprendido.

**Objetivos de esta sección:**
1. Los estudiantes escriban su primer pseudocódigo completo
2. Practiquen aplicar las 5 preguntas mágicas
3. Se familiaricen con la sintaxis antes de ver Python

**Estrategia pedagógica:**
1. **Ejercicio individual/parejas:** 5-10 minutos
2. **Compartir soluciones:** 2-3 voluntarios muestran su pseudocódigo
3. **Discusión:** Comparar enfoques diferentes (todos válidos si funcionan)
4. **Mostrar solución modelo:** Después de que hayan intentado

**Mensaje motivacional:**
"El pseudocódigo es como escribir la letra de una canción antes de componerla. Algunos la escriben directamente, pero es más fácil si primero tenés las palabras claras."

**Preparación del aula:**
- Si están en computadoras: usar editor de texto simple
- Si están en papel: mejor aún (menos distracciones)
- Recordales que pueden consultar los slides anteriores

**Criterios de evaluación (para ti, no para ellos):**
- ¿Tiene INICIO y FIN?
- ¿Está indentado?
- ¿La lógica es correcta?
- ¿Es legible?

No importa si usan ENTONCES o no, o si prefieren = en vez de ⟸. Lo importante es la lógica.

**Tiempo sugerido:**
- 1-2 min: Leer el problema
- 5-7 min: Escribir pseudocódigo
- 3-5 min: Compartir y discutir
- 2-3 min: Mostrar solución modelo

**Siguiente slide:** El problema específico que resolverán.
-->

---

<!-- _header: 'Ejercicio' -->

# Ejercicio: Número par o impar

**Problema:**
* Leer un número
* Decir si es par o impar

**Pista:**
* Un número es par si `numero % 2 == 0`
* `%` es el operador módulo (resto)

**Estructura:**
* INICIO
* LEER numero
* SI ... usar condición de arriba
* MOSTRAR resultado
* FIN

<!--
Este es el **primer ejercicio que resolverán independientemente**. Es simple pero requiere aplicar lo aprendido (entrada, decisión, salida).

**Análisis del problema:**

*Aplicando las 5 Preguntas Mágicas:*
1. ¿Qué datos necesito? → Un número
2. ¿Qué resultado quiero? → "Par" o "Impar"
3. ¿Qué pasos debo seguir? → Leer, verificar si % 2 == 0, mostrar
4. ¿Hay situaciones especiales? → ¿Qué pasa con 0? (es par)
5. ¿Necesito repetir algo? → No

**Concepto nuevo: Operador módulo (%)**
- `a % b` = resto de dividir a entre b
- Ejemplos:
  - 10 % 2 = 0 (10÷2 = 5, resto 0)
  - 11 % 2 = 1 (11÷2 = 5, resto 1)
  - 7 % 3 = 1 (7÷3 = 2, resto 1)

**Lógica del problema:**
- Si numero % 2 == 0 → el resto es 0 → es divisible por 2 → **par**
- Si numero % 2 != 0 → el resto es 1 → no es divisible por 2 → **impar**

**Solución modelo (no mostrar hasta que hayan intentado):**

```
INICIO ParOImpar
    # Entrada
    MOSTRAR "Ingrese un número:"
    LEER numero
    
    # Proceso y Salida
    SI numero % 2 == 0 ENTONCES
        MOSTRAR "El número es par"
    SINO
        MOSTRAR "El número es impar"
    FIN_SI
FIN
```

**Variaciones válidas que pueden surgir:**

*Variación 1 (guardando resultado):*
```
SI numero % 2 == 0 ENTONCES
    resultado ⟸ "par"
SINO
    resultado ⟸ "impar"
FIN_SI
MOSTRAR resultado
```

*Variación 2 (solo SI, sin SINO):*
```
SI numero % 2 == 0 ENTONCES
    MOSTRAR "Par"
FIN_SI
SI numero % 2 != 0 ENTONCES
    MOSTRAR "Impar"
FIN_SI
```
Funciona, pero el SI-SINO es más eficiente.

**Errores comunes a anticipar:**

1. **Olvidar FIN_SI:**
   ```
   SI numero % 2 == 0 ENTONCES
       MOSTRAR "Par"
   SINO
       MOSTRAR "Impar"
   # ¡FALTA FIN_SI!
   ```

2. **Usar = en vez de ==:**
   ```
   SI numero % 2 = 0 ENTONCES  ← Error: = es asignación
   ```
   En pseudocódigo es aceptable, pero en Python no.

3. **Confundir la lógica:**
   ```
   SI numero % 2 == 1 ENTONCES
       MOSTRAR "Par"  ← Error: está al revés
   ```

**Casos de prueba para verificar:**
- numero = 4 → "Par" ✓
- numero = 7 → "Impar" ✓
- numero = 0 → "Par" ✓ (0 es par por definición)
- numero = -2 → "Par" ✓
- numero = -3 → "Impar" ✓

**Extensión (si hay tiempo):**
"¿Cómo modificarían el pseudocódigo para verificar si es divisible por 3?"
→ Cambiar `numero % 2 == 0` por `numero % 3 == 0`

**Actividad:**
1. Pedí a 2-3 voluntarios que compartan su pseudocódigo (proyectado o en pizarra)
2. Discutí las diferencias
3. Mostrá la solución modelo
4. Enfatizá que hay múltiples formas correctas

**Transición:** "Ahora que escribieron pseudocódigo, están listos para ver cómo se traduce a Python (próxima presentación). Pero primero, resumen de lo aprendido."
-->

---

<!-- _header: 'Resumen' -->

# Para recordar

**Pseudocódigo:**
* Español estructurado
* Enfocado en lógica, no sintaxis
* Fácil de traducir a código real

**Estructura:**
* INICIO/FIN
* Indentación para jerarquía
* Palabras clave: SI, MIENTRAS, PARA

**Próximo paso:**
* Pruebas de escritorio

<!--
Este slide de **cierre** resume los conceptos clave de toda la presentación.

**Mensaje central:** El pseudocódigo es la herramienta de diseño que conecta el pensamiento humano con el código ejecutable.

**Repaso de conceptos clave:**

1. **¿Qué es pseudocódigo?**
   - Representación textual de un algoritmo
   - Más preciso que lenguaje natural
   - Más flexible que sintaxis de programación
   - "El mejor de ambos mundos"

2. **Ventajas del pseudocódigo:**
   - Enfoque en lógica, no en sintaxis
   - Detección temprana de errores
   - Independiente del lenguaje
   - Fácil de compartir y discutir

3. **Elementos fundamentales:**
   - INICIO/FIN: Delimitadores
   - LEER/MOSTRAR: Entrada/Salida
   - ⟸: Asignación
   - SI-ENTONCES-SINO: Decisiones
   - MIENTRAS/PARA: Lazos
   - #: Comentarios

4. **Buenas prácticas:**
   - Usar indentación consistente
   - Nombres de variables descriptivos
   - Comentarios para explicar porqué
   - Separar visualmente Entrada-Proceso-Salida

**Conexión con contenido previo:**

*Herramientas de diseño vistas hasta ahora:*
1. **Las 5 Preguntas:** Análisis del problema
2. **Diagramas de flujo:** Representación visual
3. **Pseudocódigo:** Representación textual

Todas son complementarias, no excluyentes.

**Flujo de trabajo completo:**
1. Leer el problema
2. Responder las 5 Preguntas Mágicas
3. Hacer diagrama de flujo O pseudocódigo (o ambos)
4. **Hacer pruebas de escritorio** (¡siguiente tema!)
5. Traducir a Python
6. Ejecutar y debuggear

**Próximo paso: Pruebas de Escritorio**

"Ya sabemos diseñar algoritmos con diagramas y pseudocódigo. Pero, ¿cómo sabemos si funcionan ANTES de programar? Eso lo veremos en la próxima presentación: Pruebas de Escritorio."

**Analogía de cierre:**
El pseudocódigo es como un guión de cine:
- No es la película final (código ejecutable)
- Pero tiene todos los diálogos y escenas (la lógica completa)
- Permite detectar problemas antes de filmar (antes de programar)
- Es más fácil y barato cambiar el guión que refilmar (más fácil cambiar pseudocódigo que reescribir código)

**Pregunta final reflexiva:**
"¿Por qué creen que les enseñamos pseudocódigo si van a programar en Python?"

Respuesta esperada: Porque la lógica es lo importante, Python es solo la herramienta. Si entienden la lógica, pueden programar en cualquier lenguaje.

**Tarea sugerida (opcional):**
"Elegí un problema simple de tu vida cotidiana (ej: preparar mate, atarse los cordones, hacer una compra) y escribilo en pseudocódigo. Traelo la próxima clase."

Esto refuerza que el pensamiento algorítmico aplica a TODO, no solo a matemática.
-->

---

<!-- _class: centered -->

# ¿Preguntas?

<!--
Momento de cierre y Q&A. Preguntas frecuentes y cómo responderlas:

**"¿Es obligatorio usar esta sintaxis exacta de pseudocódigo?"**
No. El pseudocódigo no tiene una sintaxis estandarizada universalmente. Diferentes libros y cursos usan variaciones. Lo importante es:
- Ser consistente dentro de tu proyecto
- Que sea legible y claro
- Que represente correctamente la lógica

**"¿En el examen puedo usar mi propia sintaxis de pseudocódigo?"**
(Consultar con la cátedra, pero generalmente): Sí, siempre que sea claro y lógicamente correcto. Pero es recomendable usar la sintaxis enseñada en el curso para evitar malentendidos.

**"¿Por qué no programamos directamente en Python?"**
Porque la programación tiene dos partes:
1. **Pensar la lógica** (lo difícil)
2. **Traducir a sintaxis** (lo fácil con práctica)

El pseudocódigo te permite enfocarte en (1) sin preocuparte por (2). Una vez que la lógica está clara, traducir a Python es trivial.

**"¿Los programadores profesionales usan pseudocódigo?"**
Depende. Algunos sí, especialmente para:
- Algoritmos complejos
- Documentación de diseño
- Comunicación con no-programadores
- Entrevistas técnicas (muchas empresas lo piden)

Pero muchos programadores expertos "piensan en pseudocódigo" mentalmente aunque no lo escriban.

**"¿Qué diferencia hay entre pseudocódigo y comentarios en código?"**
- **Pseudocódigo:** Se escribe ANTES del código, en la etapa de diseño
- **Comentarios:** Se escriben DURANTE o DESPUÉS del código, para explicarlo

El pseudocódigo es el plan. Los comentarios son la documentación.

**"¿Puedo usar pseudocódigo en español en vez de inglés?"**
Absolutamente. El pseudocódigo en español es válido (de hecho, es más claro para hispanohablantes). Algunos prefieren mezclar: palabras clave en inglés (IF, WHILE) y variables/comentarios en español. Elegí lo que te resulte más cómodo.

**"¿Tengo que hacer pseudocódigo para TODOS los problemas?"**
Cuando están aprendiendo: sí, es altamente recomendable. Con experiencia:
- Problemas simples: lo hacés mentalmente
- Problemas complejos: lo escribís en papel/pantalla

Es como usar rueditas en la bici: necesarias al principio, opcionales después.

**"¿Qué hago si mi pseudocódigo resulta estar mal después de programar?"**
¡Perfecto! Eso significa que detectaste el error en el diseño. Ahora podés:
1. Corregir el pseudocódigo
2. Actualizar el código en consecuencia

Esto es MUCHO más rápido que debuggear código sin saber dónde está el error lógico.

**"¿El pseudocódigo puede tener bugs?"**
Sí, absolutamente. Un pseudocódigo puede tener errores lógicos. Pero es más fácil detectarlos en pseudocódigo (porque es más legible) que en código con sintaxis complicada.

Por eso el siguiente paso son las **Pruebas de Escritorio**: ejecutar el pseudocódigo manualmente para verificar que funciona.

**Cierre motivacional:**
"El pseudocódigo es como aprender a nadar primero en la parte baja de la pileta. Podés concentrarte en la técnica sin preocuparte por ahogarte. Una vez que dominás la técnica, podés ir al agua profunda (código real) con confianza."

**Transición a 0.5:**
"En la próxima presentación veremos cómo PROBAR que nuestro pseudocódigo funciona, sin necesidad de programarlo. Se llama 'Pruebas de Escritorio' y es una técnica que les ahorrará horas de debugging."

**Recordatorio de tarea (si la asignaste):**
"No olviden escribir su pseudocódigo de una actividad cotidiana. Lo revisaremos la próxima clase."

**Agradecimiento:**
"Gracias por su atención. Practiquen escribiendo pseudocódigo, porque mientras más lo hagan, más natural será pensar algorítmicamente."
-->